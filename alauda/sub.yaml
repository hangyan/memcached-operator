apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: memcached-operator-subsription
  namespace: marketplace
spec:
  channel: alpha
  name: memcached-operator
  source: harbor
  sourceNamespace: marketplace
